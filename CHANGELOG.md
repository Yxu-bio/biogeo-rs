# 变更记录

本项目遵循语义化版本号。机器结果格式具有各自独立的格式号；CLI 版本变化不替代格式兼容检查。

## 0.1.0-rc.1 - 2026-08-24

### 新增

- 统一 BioGeoBEARS-like 似然引擎及 DEC、DEC+J、DIVALIKE、DIVALIKE+J、BAYAREALIKE、
  BAYAREALIKE+J 六个 preset。
- 23 行版本化参数表，支持参数固定、释放、表达式联动、多起点优化和模型嵌套关系判断。
- 静态及分时期 dispersal、距离、环境距离、面积、局部灭绝、邻接和允许范围修饰。
- 祖先范围后验、节点分裂后验、模型比较、AIC/AICc 模型平均和 likelihood-ratio test。
- 生物地理随机历史的确定性并行、分片、检查点、取消、预算停止、恢复和深度检查。
- Newick/NEXUS、古老末端、直接祖先、随机化石放置和常见 BioGeoBEARS/RASP 输入转换。
- 可移植分析结果、统一单模型/多模型工作流、机器进度与错误协议，以及新版 RASP 子进程参考宿主。
- 冻结新版 RASP v0.1 的进程、schema、生命周期和项目迁移边界，不约束 GUI 或内部项目实现。
- Windows x86_64 MSVC v3 目录包、标准 `/` 路径 ZIP、SHA-256、locked 构建来源清单、
  可选 Authenticode/CI 记录、第三方许可证、非覆盖安装和安装后科学检查。
- GitHub 仓库中文首页，包含项目定位、主要能力、可直接运行的单模型/多模型/生物地理随机历史示例和文档索引。
- 源码与许可证工程审计，明确区分 Rust 运行时、BioGeoBEARS GPL 官方测试数据、忽略的本地 R 环境和 LAGRANGE-ng 二进制。
- 整个项目采用 `GPL-3.0-or-later`，许可证全文进入源码、Windows 发布包和安装目录；发布状态允许公开分发。
- 可配置 Windows PC 长稳脚本，循环执行六模型优化、并行生物地理随机历史、深度检查、科学
  指纹和进程峰值记录。

### 验证

- 六个 preset 的 fixed likelihood、祖先/分裂后验和参数优化均有 BioGeoBEARS golden。
- 官方 Psychotria、Conifer、BSM 3-taxon 案例和 Ponerinae 1534-tip 数据进入回归或真实规模验收。
- 六 preset 的 12 项静态/两时期修饰组合与 12 项拒绝规则进入源码和安装版门禁。
- 生物地理随机历史以大量独立样本进行分布级对照，不要求不同实现逐条路径相同。
- 仓库首页的 DEC 快速入门已加入自动检查，要求优化收敛、结果精确重放、100 条生物地理随机历史完成且深度检查零违规。
- 一键科研试用版检查覆盖 locked 测试、完整科学语义、ZIP 结构、安装、真实工作流和新版 RASP
  参考宿主；D2-D4 首次并入后的完整运行用时 284.562 秒。
- 源码和安装版参考宿主均通过只读中文输入、删除源目录、跨项目移动、结果重放和移动后重新
  生成生物地理随机历史。
- 特殊树输入门禁覆盖显式缺失枝长填充、真实 NEXUS 变体、带空格/转义标签、多分叉及
  `UTREE` 拒绝，并验证可移植结果在删除源输入后仍可精确重放。
- 六 preset 各 20,000 条完整生物地理随机历史通过节点状态、分裂场景和 `y/s/v/j` 事件的
  精确后验分布门禁；官方 BioGeoBEARS 5000 对 5000 的 39 项全路径分布门禁同步复跑通过。
- 增加无分配的组合状态数预估和可选 `max_states` 资源上限；20/30 区域成功路径与
  614,429,672 状态任务的提前拒绝均进入源码、release CLI 和安装版门禁。
- 测试构建分别在随机历史行写入、表同步和 checkpoint 临时文件写入中注入 `StorageFull`；
  三条路径均清理临时检查点并回滚到已提交长度，恢复后的八张表与一次性基线逐字节一致。
- Windows 安装检查允许未签名科研包直接构建和安装；当用户主动请求签名时，仍会拒绝缺失证书、
  给未签名包指定发布者以及伪造签名元数据。
- Windows PC 两小时长稳完成 367 轮六模型优化和 1,503,232 条随机历史；17,664,484,003 bytes
  累计逻辑写入中科学指纹零漂移，最高 working set 41,398,272 bytes，所有 stderr 为空。
- 最终重建 release EXE 另跑 10 轮、40,960 条同规格随机历史，输入、请求和两个科学指纹与两小时
  证据一致；E3/E4 收口后的完整 RC 门禁 288.82 秒一次通过。

### 修复

- 移除把 Windows 代码签名和专用 CI 作为公开科研软件必要条件的过度约束。未签名包现在可直接构建和
  安装，只有用户主动提供签名参数时才验证证书和发布者指纹。
- Windows 打包时暂存目录发布仅对 access denied、sharing violation 和 lock violation 做短时
  有界重试；其他文件系统错误仍立即返回，失败清理不留下半成品。
- 规范 TSV 范围及 detection 表改为按制表符保留带空格的 tip 和区域标签；无制表符的旧式
  空白分隔输入继续兼容。

### 已知限制

- 当前发布链仅验证 64 位 Windows MSVC；Linux、Slurm、cgroup 和多进程协调后置。
- 状态数随区域数和 `max_range_size` 组合增长，20/30 区域任务可能受内存和计算量限制。
- CLI 不负责绘图、祖先范围可视化和报告，这些由新版 RASP 消费版本化结果实现。
